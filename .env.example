# API Configuration
VITE_API_URL=

# GitHub Token for submission storage (required for production)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GH_TOKEN=

# Admin Secret for server-side API auth (KEEP PRIVATE)
ADMIN_SECRET=

# Comma separated list of allowed admin usernames (optional). If set, requests must include x-admin-user header that matches.
ADMIN_ALLOWED_USERS=

# Optional: JSON array of per-user credentials (overrides ADMIN_SECRET/ADMIN_ALLOWED_USERS when set)
# Example: [{"username":"alice","password":"pass1"},{"username":"bob","password":"pass2"}]
ADMIN_USERS_JSON=
